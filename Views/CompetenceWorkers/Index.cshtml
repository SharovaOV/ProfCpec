@model IEnumerable<ProfSpec.Models.CompetenceWorker>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Level)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Competence)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Worker)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Level)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Competence.CompetenceID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Worker.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompetenceWorkerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompetenceWorkerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompetenceWorkerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
