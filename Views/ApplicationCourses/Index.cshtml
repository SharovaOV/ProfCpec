@model IEnumerable<ProfSpec.Models.ApplicationCourse>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Worker)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StagesApplication)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Worker.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StagesApplication.StagesApplicationID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ApplicationCourseID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ApplicationCourseID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ApplicationCourseID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
